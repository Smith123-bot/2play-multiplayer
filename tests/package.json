{
  "name": "@2play/tests",
  "version": "2.1.0",
  "private": true,
  "description": "2PLAY end-to-end integration tests over Socket.IO + HTTP",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@2play/shared": "*",
    "socket.io-client": "^4.8.1",
    "supertest": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.8.1",
    "@types/supertest": "^6.0.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
